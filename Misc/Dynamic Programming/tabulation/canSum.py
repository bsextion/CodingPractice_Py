import math
def canSum(targetSum, arr):
    table = [False] * 1



class Solution(object):
    def sumZero(self, n):
        arr = [x*-1 for x in range(n/2) if x % 2 != 0]
        return arr

        """
        :type n: int
        :rtype: List[int]
        """
Solution.sumZero("", 5)